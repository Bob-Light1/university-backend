'use strict';

/**
 * @file campus.service.js — API inter-modules du domaine campus.
 *
 * Vide pour l'instant : les ~32 consommateurs passent par le shim
 * models/campus.model.js — à remplacer progressivement par des fonctions
 * de service ici lors de la phase nettoyage.
 */

module.exports = {};
