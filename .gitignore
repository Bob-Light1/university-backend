node_modules
usage_guides
# User-uploaded files (PII: student/teacher photos, imported documents).
# Correct path — the previous "/api/uploads/" never matched this folder.
uploads/
# Docs are ignored except architecture design docs (no secrets — Phase 3 D12)
docs/*
!docs/architecture/
oldCode.md
.env
.env.*
!.env.example
*.log
claude.md
.claudeignore
DEPLOY_AI.md 
# Sensitive activation links/codes exported by scripts/migrate-account-activation.js
account-activation-export-*.csv

# Generated by the test fixture (CH-0): stable ids AND the clear-text test password.
tests/fixtures/.generated/
