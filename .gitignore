node_modules
usage_guides
# User-uploaded files (PII: student/teacher photos, imported documents).
# Correct path — the previous "/api/uploads/" never matched this folder.
uploads/
docs
oldCode.md
.env
.env.*
!.env.example
*.log
claude.md
.claudeignore
# Sensitive activation links/codes exported by scripts/migrate-account-activation.js
account-activation-export-*.csv
